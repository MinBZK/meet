apiVersion: v1
kind: Service
metadata:
  name: svc-livekit
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    - port: 5349
      targetPort: turn-tls
      protocol: TCP
      name: turn-tls
  selector:
    app: livekit-server
  type: ClusterIP

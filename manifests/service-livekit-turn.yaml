apiVersion: v1
kind: Service
metadata:
  name: svc-livekit-turn
spec:
  ports:
    - port: 443
      targetPort: turn-tls
      protocol: TCP
      name: turn-tls
  selector:
    app: livekit-server
  type: LoadBalancer
